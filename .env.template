# Environment
NODE_ENV=development
WEBSITE_URL=http://localhost:3000
APPLICATION_ID=

# Better Auth - Configuration
BETTER_AUTH_SECRET=

# Database (Turso)
DATABASE_URL=
DATABASE_AUTH_TOKEN=

# Gateway Configuration
AI_GATEWAY_BASE_URL=
AI_GATEWAY_API_KEY=

# Payments and tracking
AUTUMN_SECRET_KEY=

# Buscador de direcciones (autocompletar en el formulario, paso 1)
GEOAPIFY_API_KEY=

# Envío del email con la valoración al usuario
RESEND_API_KEY=

# Panel de administración (/admin) — la contraseña se valida en el
# servidor, nunca se envía al navegador. Cámbiala por una propia.
ADMIN_PASSWORD=
# Secreto para firmar las sesiones del panel de administración. Genera uno
# aleatorio y largo (por ejemplo: openssl rand -hex 32). Si se deja vacío
# se usa ADMIN_PASSWORD como secreto, lo cual es menos seguro.
ADMIN_SESSION_SECRET=
